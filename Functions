#include<iostream>
using namespace std;
int power(int a, int b){
    int ans=1;
    for(int i=0;i<=b;i++){
        ans=ans*a;
    }
    cout<<"power of a is"<<ans<<endl;
}

int main()
{
    cin>>a>>b;
    power(a , b);
}